{
  "name": "server",
  "version": "1.0.0",
  "description": "This project demonstrates how to create a full-stack web application with a React frontend and a Node.js (Express) backend. The backend securely handles sensitive operations like API requests with secret keys, while the React frontend interacts with the backend.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.20.0",
    "googleapis": "^144.0.0"
  }
}
